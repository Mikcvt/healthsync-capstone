import 'package:flutter/material.dart';

class AddMedicineStep1Screen extends StatelessWidget {
  const AddMedicineStep1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add medicine - Step 1')),
      body: const Center(child: Text('Add medicine step 1 screen')),
    );
  }
}
