import 'package:flutter/material.dart';

class AddMedicineStep2Screen extends StatelessWidget {
  const AddMedicineStep2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add medicine - Step 2')),
      body: const Center(child: Text('Add medicine step 2 screen')),
    );
  }
}
