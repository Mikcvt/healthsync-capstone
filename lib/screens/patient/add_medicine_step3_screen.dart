import 'package:flutter/material.dart';

class AddMedicineStep3Screen extends StatelessWidget {
  const AddMedicineStep3Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add medicine - Step 3')),
      body: const Center(child: Text('Add medicine step 3 screen')),
    );
  }
}
